# 第一批进度

更新时间：2026-09-18（Asia/Shanghai）

## 当前状态

- 开发：已完成本地代码实现和生产构建；已形成本地提交，尚未发布。
- 测试：静态检查和本地浏览器冒烟通过；真实 U1/U2、RLS、并发、迁移后写入尚未执行。
- 发布：未发布。线上仍为 `604607f49143700f65bc7ce7b136e209d770beb4`。

## 已完成

- 服务端论坛分页（每页 20）、标题/正文搜索、分类筛选、最新/最热排序和 URL 保留。
- 帖子/回复长度、分类和空白校验；安全 Markdown 预览与展示。
- 登录安全回跳、guest/user-id 草稿隔离、提交中禁用、请求 ID 和数据库幂等设计。
- 本人帖子/回复编辑与软删除；投票互斥、去重、成功后重新读计数。
- 找回密码和重设页面；向量接口要求登录作者且缺配置时返回 503，不影响发帖。
- 兼容迁移：软删除、更新时间、请求 ID、限流日志、投票唯一索引/计数触发器、回复计数触发器、RLS。

## 当前阻塞

1. 本机只有 Supabase URL/anon key，没有隔离测试账号、service-role key、邮件捕获配置或数据库管理入口；迁移不能安全地执行到现有演示项目。
2. 没有已确认的第二批管理员需求，本批不涉及管理员初始化。
3. 当前浏览器工具没有 390px viewport 控制，因此只能完成默认视口横向溢出检查，不能声称真机/390px 验收。

## 下一步

在用户提供安全的隔离 Supabase/测试账号入口后，执行迁移并完成 AT01–AT22 中被阻塞的身份、RLS、并发、找回邮件和性能用例；迁移前先做隔离备份，再决定是否发布。
