# 第一批测试报告

提交 SHA：以交付时 `git rev-parse HEAD` 为准。执行时间：2026-09-18（Asia/Shanghai）。

## 自动检查

| 检查 | 命令 | 结果 |
|---|---|---|
| lint | `npm run lint` | 通过，退出码 0 |
| TypeScript | `npx tsc --noEmit` | 通过，退出码 0 |
| 生产构建 | `npm run build` | 通过，退出码 0；Next.js 16.2.12 production build |
| diff 检查 | `git diff --check` | 通过；仅有 Git 的 LF/CRLF 提示 |

## 本地浏览器证据

环境：本机 Next dev，`http://127.0.0.1:3000`，匿名会话，默认视口 821px；未写入数据库。

- `/forum`：列表实际显示帖子、作者、回复数、赞/踩数；服务端返回 5 条样例。
- 搜索 `AI Agent` 后 URL 为 `/forum?q=AI+Agent`，结果从 5 条缩为 2 条，证明不是只过滤当前页。
- `/forum/999999`：显示“帖子不存在、已删除或暂时不可见”，没有永久加载。
- `/forum/new` 空提交：标题/正文分别显示字段错误，输入未被清空。
- Markdown 预览：加粗、列表、代码块正常；`javascript:` 链接以纯文本显示，未生成可点击危险链接。
- 未登录提交合法草稿：跳转 `/login?returnTo=...`；外部 `returnTo=https://evil.example/steal` 被归一为 `/`。
- `/forum/10`：详情、Markdown 回复、投票展示、回复输入区正常；无浏览器控制台错误。
- `/forum/10`：每条评论显示独立 👍/👎 控件；匿名点击会进入登录回跳。作者“编辑/删除”入口已保留，真实保存需认证账号和迁移后验证。
- `/skills`、`/agents`、`/`：原有页面可访问，标题/列表可见。
- 默认视口 `window.innerWidth=821` 时 `scrollWidth=bodyWidth=821`，未见页面级横向溢出。工具不支持 390px viewport override，AT19 手机尺寸未宣称通过。

## AT01–AT22

| 用例 | 状态 | 证据/说明 |
|---|---|---|
| AT01 | 阻塞 | 需隔离 U1、迁移后真实插入/重登；未写线上数据 |
| AT02 | 部分通过 | 本地空值校验已实测；边界 Unicode 与数据库约束待迁移后执行 |
| AT03 | 阻塞 | 代码含 request_id/upsert；需真实数据库唯一索引和响应丢失注入 |
| AT04 | 通过（导航部分） | 本地安全回跳与草稿 key 代码/浏览器验证；双账号隔离待认证环境 |
| AT05 | 阻塞 | 编辑/RLS 已实现，需 U1/U2 真实身份验证 |
| AT06 | 阻塞 | 软删除、帖子/回复计数和评论投票触发器已写迁移，未在线执行 |
| AT07 | 阻塞 | 线上匿名不可写；需隔离账号投票四步 |
| AT08 | 阻塞 | 帖子和评论投票均需真实并发身份和唯一索引 |
| AT09 | 部分通过 | 客户端错误不清空输入/不伪造计数已实现；需故障注入 |
| AT10 | 部分通过 | 列表服务端获取和详情重新读计数已实现；互动后 3 秒真实测量待隔离环境 |
| AT11 | 通过（浏览器预览） | 安全 Markdown 预览实测；发布后详情一致待写入 |
| AT12 | 部分通过 | 不展示邮箱、作者字段/匿名回退已实现；新账号昵称需隔离测试 |
| AT13 | 通过（搜索路径） | URL 搜索/筛选/排序路径实测；200 帖翻页需性能/数据夹具 |
| AT14 | 通过（不存在路径） | 无效 ID 提示实测；超时/断网恢复待注入 |
| AT15 | 阻塞 | 登录页面和会话处理已实现；需测试账号验证过期/退出写入 |
| AT16 | 阻塞 | 页面已实现；无隔离邮箱捕获渠道，不能声称邮件送达 |
| AT17 | 阻塞 | RLS 迁移待执行，未做越权写入尝试 |
| AT18 | 阻塞 | 限流/RPC/向量锁已写；需真实 DB 和调用计数替身 |
| AT19 | 部分通过 | 默认视口/键盘表单/控件标签实测；390px/Chrome+Edge 未完成 |
| AT20 | 通过（冒烟） | Skills、Agents、首页本地可访问；原数据未写入改动 |
| AT21 | 未执行 | 没有隔离 200 帖/1000 回复和 10 并发条件 |
| AT22 | 未执行 | 未迁移、未发布，不能伪称备份恢复/回滚通过 |

## 结论

当前是“开发完成、部分本地验收通过”，不是第一批测试完成。必需的真实身份、迁移、RLS、并发、邮件、性能和发布用例仍阻塞整批完成。
