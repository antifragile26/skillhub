# 第二批发布记录

## 当前结果

- 状态：自动审核 Agent v1 已发布；完整第二批验收未通过。
- 当前线上：`http://47.116.109.213/forum`
- 当前线上版本：`2b907fa`
- 上一稳定版本：`4fd9e485d672c69b91f4e4ba46843c5f00134447`，仍保留在 releases 目录，可用于应用回滚。
- 上一应用版本：`5954e937f3f89d6fd370d2903b4851fc7d223b16`，仍保留在 releases 目录。

## 发布门槛

1. 执行并结构核验 `20260918020000_batch_2_operations.sql`：已完成；数据量和策略快照已记录。
2. 确认管理员/运营测试账号，完成 BT01–BT27 与第一批受影响回归：未完成。
3. 记录并确认私有备份标识，演练兼容回滚：私有快照执行成功；应用 release 已保留，数据库不做反向删除。
4. 仅打包已测试提交，建立隔离 release，切换 current 后核对 `/api/health`、PM2 cwd 和论坛/知识路由：已完成。

## 回滚

应用回滚到上一稳定 release；数据库保留新增结构，不执行未经验证的反向删除。若审核状态与旧应用不兼容，先关闭公开读取或恢复兼容版本，不能直接放开隐藏内容。
