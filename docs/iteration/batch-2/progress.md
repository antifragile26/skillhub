# 第二批进度

更新时间：2026-09-18（Asia/Shanghai）

## 当前状态

- 开发：核心数据库兼容层、论坛审核状态、案例/问答、知识库/专题、通知、产品关联和运营后台已实现，待迁移后的真实联调。
- 测试：本地 lint、TypeScript、生产构建已通过；数据库/RLS/角色/并发/完整浏览器链路尚未通过。
- 发布：未发布第二批。线上仍运行第一批提交 `4fd9e485d672c69b91f4e4ba46843c5f00134447`。

## 已完成

- 新增 `20260918020000_batch_2_operations.sql`：角色、禁言、审核状态、举报、审计、精选置顶、案例字段、采纳、知识、专题、产品关联、通知、活动统计和受保护函数。
- 新帖提交进入待审核；驳回稿可编辑重提；已发布内容修改后进入待审核；公开读取由数据库状态控制。
- 新增 `/knowledge`、`/knowledge/[id]`、`/collections`、`/collections/[id]`、`/notifications`、`/admin`。
- 后台包含审核、举报、精选/置顶、知识草稿编辑/发布、专题维护、产品关联、角色/禁言和基础统计入口。
- 论坛支持结构化案例字段、问题采纳、举报和内容状态标识。

## 当前阻塞

1. 第二批迁移还没有在 Supabase 项目 `klkpnrkmfsvelghzplvw` 执行并核验；不能把仓库 SQL 文件当成线上生效。
2. 没有用户明确指定的管理员/运营账号，不能自动给未知账号授予角色；因此 BT01/BT02、后台真实操作和完整链路无法判通过。
3. 没有隔离 U1/U2/O/M、邮件捕获和性能夹具；BT03–BT27 的真实身份、RLS、并发、通知、统计和性能证据待补。
4. GitHub HTTPS 推送曾出现连接重置；第一批通过授权 SCP 发布，第二批需沿用同一发布路径并记录 SHA。

## 下一步

执行第二批迁移并做结构/RLS 快照；由用户在安全入口确认一个管理员和一个运营测试账号后，完成隔离验收、修复缺陷、备份后部署和线上冒烟。
